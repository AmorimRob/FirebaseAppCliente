<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppCliente.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AppCliente"
    Title="Xamarin Saturday">

    <StackLayout Margin="10">

        <Label Text="Cliente" />
        <Entry Text="{Binding Pedido.Cliente}" />

        <Label Text="Produto" />
        <Entry Text="{Binding Pedido.Produto}" />

        <Label Text="Preço" />
        <Entry Text="{Binding Pedido.Preco}" />

        <Button
            Command="{Binding EnviarPedidoCommand}"
            HorizontalOptions="FillAndExpand"
            Text="Enviar Pedido"
            VerticalOptions="EndAndExpand" />

    </StackLayout>

</ContentPage>
